
function checkEmailAvailable(email) {
    return true;
}

function checkEmailValid(email) {
    return true;
}

function sendVerificationEmail(email) {

}

function checkPhoneAvailable(phone) {
    return true;
}

function checkPhoneValid(phone) {
    return true;
}

export default {
    checkEmailAvailable, checkEmailValid, sendVerificationEmail, checkPhoneAvailable, checkPhoneValid
}